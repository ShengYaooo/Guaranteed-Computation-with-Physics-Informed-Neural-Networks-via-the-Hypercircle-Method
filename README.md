# Thesis Code

This repository contains the training and evaluation code used in my thesis.

## Environment
Create a virtual environment and install dependencies:

### pip/venv
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

## Run
Example:
python pinn_disk_hypo.py
